def mincost(G, start, cost):
    """
    todo[start] = 0
    while todo:
      (cur, c) = todo.pop_smallest()
      mincost[cur] = c
      for n in neighbors:
        if n in mincost: continue
        if (n not in todo) or todo[n] > c + cost((c,n)):
          todo[n] = c + cost((c,n))
    """
    pass

